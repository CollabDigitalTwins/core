  ctx.register('ui.dialogs', {
    id: '{{SLUG}}',
    // Looked up in this plugin's own message namespace, falling back to the literal.
    titleKey: '{{NAME}}',
    size: 'lg',
    component: {{COMPONENT}},
  })
