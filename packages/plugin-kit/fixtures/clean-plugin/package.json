{
  "name": "@collabdt/fixture-clean-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Fixture: a plugin whose every import is on the kit's allowlist. Built by src/build.test.ts.",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@collabdt/plugin-kit": "file:../..",
    "@types/react": "^19.2.2",
    "maplibre-gl": "^5.24.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
